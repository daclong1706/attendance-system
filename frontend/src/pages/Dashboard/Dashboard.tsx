import { Button } from "flowbite-react";
import React from "react";

const Dashboard = () => {
  return (
    <div className="bg-amber-900 dark:bg-amber-50">
      Dashboard
      <Button color="alternative">Alternative</Button>
    </div>
  );
};

export default Dashboard;
